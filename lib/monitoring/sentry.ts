export function initSentry() {
  // Placeholder for Sentry init (server + client)
  // Sentry SDK can be added and initialized here with DSN from env
  if (process.env.NODE_ENV === "development") {
    // no-op
  }
}
